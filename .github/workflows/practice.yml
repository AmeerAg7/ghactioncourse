name: trigger type workflow

on: 
  push:
    branches:
      - test

jobs:
  testing_example:
    runs-on: ubuntu-latest

    steps:
      - name: test1before
        run: echo "test 1 before push passed !!"

      - name: test2after
        run: echo "test 2 after push passed !!!"

      - name: Checkout code
        uses: actions/checkout@v6.0.2

      # - name: display test.sh code
      #   run: cat test.sh

      - name: display current directory
        run: pwd
